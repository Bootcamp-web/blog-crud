<div class="card" style="margin: 10px">
	<img src="/staticFiles/img/{{img}}" class="card-img-top" alt="..." />
	<div class="card-body">
		<h5 class="card-title">{{name}}</h5>
		<p class="card-text">Benefits: {{benefits}}</p>
		<a href="/remove?id={{_id}}" class="btn btn-primary">Delete entry</a>
	</div>
</div>